-- gallery_layout: вариант представления финальной галереи на странице
-- кейса (решения (51)-(52) 2026-09-14): editorial (дефолт, сетка с кропом
-- в слоты) / masonry (колонки) / justified (равновысокие ряды) —
-- оригинальные пропорции кадров.
-- Nullable: отсутствие значения = editorial (совместимо с существующими
-- проектами, дефолт подставляется в коде).
ALTER TABLE `projects` ADD `gallery_layout` text;